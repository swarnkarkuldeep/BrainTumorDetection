export default function Footer() {
  return (
    <footer className="footer">
      <p>
        Educational demo only — not a medical device. It does not produce a diagnosis.
        Consult a qualified radiologist for real evaluation.
      </p>
    </footer>
  );
}
